import { Tool } from '@modelcontextprotocol/sdk/types.js';
import { OS_GUIDANCE, CHAIN_GUIDANCE, SUPPORTED_CHAINS } from '../constants/guidance.js';

export const BLOCKS_TOOLS: Tool[] = [
  // Note: evm_get_block_number and evm_get_block are already included in JSON_RPC_TOOLS and CORE_TOOLS respectively
];
