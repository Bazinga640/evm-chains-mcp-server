import { Tool } from '@modelcontextprotocol/sdk/types.js';
import { OS_GUIDANCE, CHAIN_GUIDANCE, SUPPORTED_CHAINS } from '../constants/guidance.js';

export const ENCODING_TOOLS: Tool[] = [
  // Note: evm_decode_contract_data and evm_encode_contract_data
  // are already included in CONTRACTS_TOOLS
];
