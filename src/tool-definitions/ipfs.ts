import { Tool } from '@modelcontextprotocol/sdk/types.js';

// Note: evm_create_nft_with_ipfs is defined in nft.ts to avoid duplication
// IPFS functionality is integrated into NFT tools rather than as a separate category

export const IPFS_TOOLS: Tool[] = [];
