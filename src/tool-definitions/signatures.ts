import { Tool } from '@modelcontextprotocol/sdk/types.js';

// Note: evm_sign_message is defined in wallet.ts to avoid duplication
// Signature tools are integrated into wallet operations rather than as a separate category

export const SIGNATURES_TOOLS: Tool[] = [];
